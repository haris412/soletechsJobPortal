import { Route } from '@angular/router';
import { CompetenciesComponent } from './competencies.component';

export const CompetenciesRoutes: Route[] = [
    {
        path     : '',
        component: CompetenciesComponent
    }
];